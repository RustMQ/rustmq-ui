import React, { Component } from 'react';
import { withRouter } from 'react-router-dom';

class Queue extends Component {
    render() {
        return <p>Queue container</p>
    }
}

export default withRouter(Queue);
